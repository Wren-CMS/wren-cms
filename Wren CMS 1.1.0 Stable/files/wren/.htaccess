# Wren CMS — optional pretty URLs.
# Upload this alongside index.php, then turn on "Pretty URLs" in Settings.
Options -Indexes
<Files "wren.db">
  Require all denied
</Files>
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?q=$1 [QSA,L]
